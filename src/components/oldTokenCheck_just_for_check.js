import { AsyncStorage } from 'react-native';
import * as AppState from '../modules/AppState';

export default (reduxCurrentToken) => {
  console.warn('here is oldTokenCheck, using reduxCurrentToken: ', reduxCurrentToken);
}
